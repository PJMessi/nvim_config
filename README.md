1. First install [Packer](https://github.com/wbthomason/packer.nvim). 

2. Set the config with the following command:
```
mkdir -p ~/.config && git clone https://github.com/PJMessi/nvim_config.git ~/.config/nvim

```
